// The Worker serves both this frontend and /api on the same origin.
// Keep this empty unless the API is deliberately deployed on another origin.
window.APP_CONFIG = Object.freeze({
    apiBaseUrl: ''
});
